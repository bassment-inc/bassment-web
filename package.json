{
  "name": "bassment-web",
  "private": true,
  "scripts": {
    "build": "node scripts/build-seo.mjs",
    "deploy": "firebase deploy --only hosting"
  },
  "devDependencies": {
    "firebase-tools": "latest"
  }
}
